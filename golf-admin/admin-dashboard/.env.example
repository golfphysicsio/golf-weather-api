# Golf Physics Admin Dashboard - Environment Configuration
# Copy this to .env and fill in your values

# Google OAuth Client ID (get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# API Base URL - LEAVE BLANK to use window.location.origin (recommended)
# This allows the dashboard to work on any domain (staging, production, localhost)
# Only set this if you need to point to a different backend
# VITE_API_BASE_URL=

# Examples for manual override (NOT recommended):
# Development: VITE_API_BASE_URL=http://localhost:8000
# Staging: VITE_API_BASE_URL=https://golf-weather-api-staging.up.railway.app
# Production: VITE_API_BASE_URL=https://golf-weather-api-production.up.railway.app
